collections:
  notes:
    permalink: /:categories/:title:output_ext	
    output: true

  posts:
    permalink: /:categories/:title:output_ext	

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: "notes"
      type: "pages"
    values:
      layout: "post"